dependencies:
    override:
        - pip install awscli
deployment:
  production:
    branch: master
    commands:
      - aws s3 sync /Users/trevor/Documents/GitHub/trevoryoung.co s3://trevoryoung.design --delete
